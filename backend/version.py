# Zentrale App-Version. Liegt in backend/ (wird bei Updates mitkopiert),
# NICHT in config/ (das bleibt beim Update erhalten und wuerde sonst eine
# veraltete Versionsnummer behalten).
APP_VERSION = "2.2.5"
